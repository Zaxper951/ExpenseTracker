const Title = () => {
  return (
    <nav className="navbar navbar-dark bg-dark d-flex justify-content-center">
      <span className="navbar-brand mb-0 h1">Simple Expense Tracker</span>
    </nav>
  );
};

export default Title;
